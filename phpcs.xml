<?xml version="1.0"?>
<ruleset name="ProjectRules">
    <description>Coding standards project</description>

    <!-- Define standard -->
    <rule ref="PSR12" />

    <!-- Include the RequireStrictTypesSniff -->
    <rule ref="Generic.PHP.RequireStrictTypes" />

    <!-- Include directories to analyze -->
    <file>src/</file>
    <file>tests/</file>

    <!-- Ignore specific files or directories -->
    <exclude-pattern>vendor/*</exclude-pattern>
</ruleset>
